import { UpdateMilestoneReq } from './update-milestone-req';

describe('UpdateMilestoneReq', () => {
  it('should create an instance', () => {
    expect(new UpdateMilestoneReq()).toBeTruthy();
  });
});
